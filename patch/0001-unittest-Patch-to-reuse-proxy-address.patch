From ebe9524bc863b6fa5ca459b336c40e20b175948d Mon Sep 17 00:00:00 2001
From: Tao Sauvage <sauvage.tao@gmail.com>
Date: Mon, 23 May 2016 09:25:49 +0200
Subject: [PATCH] [unittest] Patch to reuse proxy address

---
 framework/core.py | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/framework/core.py b/framework/core.py
index 53b9adc..83f8ffb 100644
--- a/framework/core.py
+++ b/framework/core.py
@@ -137,8 +137,9 @@ class Core(BaseComponent):
                     int(self.db_config.Get('INBOUND_PROXY_PORT'))))
                 temp_socket.close()
             except socket.error:
-                self.error_handler.FrameworkAbort("Inbound proxy address %s:%s already in use" %
-                    (self.db_config.Get('INBOUND_PROXY_IP'), self.db_config.Get("INBOUND_PROXY_PORT")))
+                #self.error_handler.FrameworkAbort("Inbound proxy address %s:%s already in use" %
+                #    (self.db_config.Get('INBOUND_PROXY_IP'), self.db_config.Get("INBOUND_PROXY_PORT")))
+                pass
 
             # If everything is fine.
             self.ProxyProcess = proxy.ProxyProcess()
-- 
2.8.2

